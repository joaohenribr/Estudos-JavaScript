const notas = [7, 7, 8, 9];
const novaListaNotas = [...notas, 10];

// novaListaNotas.push(10);

console.log(notas);
console.log(novaListaNotas);

// criar um array totalmente novo, com a ajuda do spread operator ... (ou operador de espalhamento).